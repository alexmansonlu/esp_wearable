# WIRING.md — 面包板接线与首次上机检查指南

> 适用对象：手上已有 ESP32（**micro-USB 版**）、面包板、杜邦线、思知瑞 AD8232 心电模块、思知瑞 GSR 皮肤电模块、PulseSensor 脉搏传感器。
> 本文档依据厂商资料（已下载到 [docs/datasheets/](docs/datasheets/)）逐一核对了 [HARDWARE.md](HARDWARE.md) 第 7 节的接线总表和 [firmware/](firmware/) v0.2.0 固件，**以本文档为准的差异在 §7 列出**。
> 文档版本：v1.0（2026-09-07）

---

## 0. 三句话结论

1. **micro-USB 版 ESP32 可以直接用。** 固件的 `board = esp32dev` 就是 ESP32-WROOM-32 模组通用配置，micro-USB 和 Type-C 版只差 USB 接口和串口芯片，引脚编号完全一样。需要的只是一根**能传数据的** micro-USB 线和 CH340（或 CP2102）驱动。
2. **固件代码与三个模块的引脚、电平、极性全部对得上**，可以直接烧录；编译结果见 §6。
3. **皮肤电模块不是 Grove GSR，是思知瑞自家的 3 针 GSR 模块**（丝印 `GND / VCC / ADC`，指套走 3.5mm 音频插孔）。接线方式一样（VCC 接 3V3），但 HARDWARE.md 里"板上电位器调基准"这句对这块板不成立——它没有电位器。

---

## 1. 已下载的资料清单

全部放在 [docs/datasheets/](docs/datasheets/)：

| 目录 | 内容 | 来源 |
|---|---|---|
| `esp32_wroom32_ch340/` | 芯路城 ESP32-WROOM-32 核心板资料包（59MB zip 已解压）：开发板**原理图**、尺寸图、Arduino IDE 使用教程、CH340 驱动、乐鑫 ESP32 中文 datasheet / 管脚清单 / 硬件设计指南、27 个 Arduino 实验例程 | [xinlucity 资料 #208](https://www.xinlucity.com/?s=resourcedetail/index/id/208.html) |
| `ad8232/` | AD8232 模块原理图（思知瑞）、电极贴位置图、ADI 官方 AD8232 datasheet、思知瑞页面全文（含 Arduino 示例代码、FAQ） | [思知瑞 AD8232 页](https://sichiray-tech.yuque.com/dm0eyv/chanpin/xcc21h2u7ps3dird) |
| `gsr/` | 模块实物图（引脚丝印）、套件接线图、正常/受干扰波形图、思知瑞 V1 与 V2 页面全文（含示例代码） | [思知瑞 GSR V1 套件页](https://sichiray-tech.yuque.com/dm0eyv/chanpin/iqepdr0qglekrtc3) |
| `pulsesensor/` | 引脚说明图、思知瑞页面全文（技术规格）、PulseSensor 官方 Arduino 库说明 | [思知瑞 PulseSensor 页](https://sichiray-tech.yuque.com/dm0eyv/chanpin/sonzrn48r5gn29pa) |

**没能下载的**（语雀附件需要登录，命令行抓不到，浏览器登录语雀账号后点击即可）：
- AD8232：`AD8232与Arduino接线图.pdf`、`单模块心电说明书(AD8232).pdf`、中文芯片手册（英文原版已从 ADI 官网下到）
- PulseSensor：`PulseSensor原理图.pdf`、`PulseSensor说明书.pdf`、`arduino示例源码.rar`
- 这些都是"锦上添花"，接线和规格信息页面文字里已经齐全。

**你给的链接对照**（有两个不是皮肤电）：
- `wn9c4sg326cpdukb` = 思知瑞「新手必读教程」目录页，不是产品页
- `pt3n3rd8w0bkh3c3` = **AD8232 心电传感器**目录页
- `gycc5ygqziexfd4w` = 皮肤电传感器 V1 目录页 ✅
- `alqll38tb7c8hdni` = PulseSensor 目录页 ✅
- MPU6050 没有给链接，本文档沿用 HARDWARE.md 第 5 节（引脚标准，不需要厂商资料）。

---

## 2. ESP32 开发板（micro-USB 版）

### 2.1 怎么确认是同一种板

芯路城原理图标题是「ESP-WROOM-32 核心板（CH340C）」，图上画的是 Type-C 口，但**核心部分与所有 ESP32 DevKitC 类板完全相同**：ESP32-WROOM-32 模组 + CH340 串口 + AMS1117 3.3V 稳压 + EN/BOOT 两个按键 + 自动下载电路。你的 micro-USB 板只要满足以下任一条，固件都不用改：

- 模组金属屏蔽罩上印 `ESP32-WROOM-32`（或 `-32D` / `-32U` / `-32E`）
- 两排排针，每排 19 针（38 针版）或 15 针（30 针版）
- 板上有 `EN` 和 `BOOT`（或 `IO0`）两个按键

固件用到的 9 个引脚（3V3、GND、21、22、32、34、35、27、14）在 38 针和 30 针版上**都有**。

### 2.2 驱动与串口

| 项目 | 说明 |
|---|---|
| 串口芯片 | 看板子背面/USB 口旁的小芯片丝印：`CH340` → 用资料包里 `03-开发工具/CH340驱动/CH341SER/SETUP.EXE`（Windows）；`CP2102` → Silicon Labs 官网驱动。macOS 12+ 两种通常免驱 |
| 串口名 | Windows：设备管理器 `COMx`。macOS：`/dev/cu.usbserial-xxxx`（CH340）或 `/dev/cu.SLAB_USBtoUART`（CP2102）。查看：`~/.platformio/penv/bin/pio device list` |
| USB 线 | **必须是数据线**。很多 micro-USB 线是纯充电线（只有 2 根线），插上灯亮但电脑不识别。换线是"找不到串口"最常见的解法 |
| 烧录 | PlatformIO 点 Upload。若卡在 `Connecting....`，按住 `BOOT` 键不放直到开始写入（部分板自动下载电路不灵） |
| 供电 | 只从 USB 供电即可，板上 3V3 稳压器给全部传感器供电绰绰有余（三个模拟模块 + MPU6050 总电流 < 30mA） |

### 2.3 面包板放置（这是新手最常卡住的地方）

芯路城尺寸图给出两排排针间距 **25.27mm（约 1 英寸）**。标准 830 孔面包板中缝两侧各 5 孔，总跨度只有 0.9 英寸——**38 针宽板插不进单块面包板**（插进去两侧都没有空孔可接线）。

三种解决方法（推荐第一种）：

1. **ESP32 不上面包板**：ESP32 单独放旁边，用**母对公**杜邦线从 ESP32 排针直接引到面包板；传感器模块插在面包板上，用面包板的电源轨分配 3V3 和 GND。最省事、不会插坏排针。
2. 两块面包板并排，拆掉相邻的一条电源轨，让 ESP32 横跨两块板中缝。
3. 若是 30 针版（排针间距约 0.9 英寸），可以直接插单块面包板，每侧留一列孔。

---

## 3. 各模块引脚与接线（按实物丝印核对）

### 3.1 供电总原则

- **全部传感器 VCC 接 ESP32 的 `3V3` 引脚**，绝不接 `5V`/`VIN`。
- ESP32 ADC 输入上限 3.3V，超过会损伤引脚。三个模块在 3.3V 供电下输出都不会超过 3.3V。
- 所有 GND 接在一起（面包板 GND 轨），否则模拟读数会乱飘。

### 3.2 思知瑞 GSR 皮肤电模块（3 针）

实物见 [docs/datasheets/gsr/V2_产品图.png](docs/datasheets/gsr/V2_产品图.png)，丝印 `GND / VCC / ADC`（V1 套件板丝印是 `GND / VCC / OUT`，同一排列）。

| 模块引脚 | 接 ESP32 | 备注 |
|---|---|---|
| `GND` | GND | |
| `VCC` | **3V3** | 厂商标 3.3V/5V 双模式；ESP32 上只能用 3.3V |
| `ADC` / `OUT` | **GPIO34** | ADC1_CH6，纯输入引脚 |
| `BAT+` / `BAT-`（如有） | 不接 | 电池版才用，用 ESP32 供电时悬空 |
| 3.5mm 插孔 | 指套线 | **要用力插到底**（厂商特别强调）；两个指套戴同一只手食指、中指第二指节 |

- 厂商电压换算："读数 ÷ ADC 满量程 × 参考电压 ÷ 2 级放大"——说明模块内部有 2 倍放大，3.3V 供电时输出可能接近 3.3V 满量程，固件的 `quality` bit1（GSR 在量程内）就是为此设的。
- **没有电位器**，不需要也不能"调基准"；基线靠固件 30 秒校准（`calibrate` 指令）。
- 厂商滤波器 0.5–5Hz 带通（V2 说明），所以读值是"变化量"而非绝对电阻——和架构里"一切用相对基线变化量"完全一致。
- 厂商提供的正常波形/受电源干扰波形对比图在 `gsr/V2_正常波形1.png` 与 `gsr/V2_电源干扰波形.png`，上机时可对照。

### 3.3 PulseSensor 脉搏传感器（3 线）

实物引脚见 [docs/datasheets/pulsesensor/引脚说明.png](docs/datasheets/pulsesensor/引脚说明.png)，圆板背面焊点从左到右 `S / + / -`。

| 焊点 / 线色 | 接 ESP32 | 备注 |
|---|---|---|
| `S`（紫） | **GPIO35** | ADC1_CH7，纯输入 |
| `+`（红） | **3V3** | 厂商规格 3.3–5.5V，3.3V 供电时输出 0–3.0V，正好在 ADC 量程内 |
| `-`（黑） | GND | |

- 板上绿光 LED 亮 = 供电正常。
- 佩戴：指尖用魔术贴轻贴，或耳垂夹。**力度是关键**——压太紧血流被压闭没波形，太松漏光噪声大。厂商建议手指上贴合后静置几秒再看波形。
- 光学面（LED 那面）避免强光直射。传感器背面元件裸露，**不要让背面碰到面包板金属或手指汗液**，可以贴一小块胶带绝缘。

### 3.4 思知瑞 AD8232 心电模块（6 针 + 3 电极）

原理图见 [docs/datasheets/ad8232/AD8232模块原理图.jpeg](docs/datasheets/ad8232/AD8232模块原理图.jpeg)。JP3 六针从 1 到 6：

| 模块引脚 | 接 ESP32 | 备注 |
|---|---|---|
| `GND` | GND | |
| `3.3V` | **3V3** | 芯片工作电压 2.0–3.5V，**只能接 3.3V** |
| `OUTPUT` | **GPIO32** | ADC1_CH4，心电模拟波形，静息电平约 1.65V（芯片 REFOUT = VS/2） |
| `LO-` | **GPIO14** | 导联脱落检测，电极掉了输出高电平 |
| `LO+` | **GPIO27** | 同上 |
| `SDN` | 不接 | 模块上 R15 10kΩ 已上拉到 3.3V，悬空 = 常开 |

电极接口：JP2 是 3.5mm 三芯插孔接原装三线电极线；JP1 是 `RL / LA / RA` 三针备用。

**电极贴法**（思知瑞版线色，见 `ad8232/电极贴位置.png`）：

| 线色 | 位置 | 对应 |
|---|---|---|
| 红 | 右胸（右锁骨下方） | RA |
| 绿 | 左胸（左锁骨下方） | LA |
| 橙/黄 | 右下腹 | RL（参考/右腿驱动） |

- 线色对应可能因批次不同，**以厂商说明书为准**；判断方法在 §5 第 7 步（R 峰应向上）。
- 电极贴片是**耗材**，贴过一次的粘性和导电胶都退化。皮肤先用酒精棉片擦拭。
- 模块上 D1 红色 LED 随心跳闪烁，是最直观的"贴对了"指示。

### 3.5 MPU6050（未变，沿用 HARDWARE.md）

VCC→3V3，GND→GND，SDA→GPIO21，SCL→GPIO22，其余悬空。**这次没有 MPU6050 也能测**：固件启动时探测失败只会在 boot 行输出 `"mpu6050":0`，`acc` 恒为 0，其他一切正常。

---

## 4. 面包板布局总表

```
                     ESP32 (micro-USB, esp32dev)
                     ┌──────────────────────┐
   面包板 + 轨 ◄──── │ 3V3                  │
   面包板 − 轨 ◄──── │ GND                  │
                     │ GPIO34 ◄──── GSR  ADC│
                     │ GPIO35 ◄──── Pulse S │
                     │ GPIO32 ◄──── AD8232 OUTPUT
                     │ GPIO27 ◄──── AD8232 LO+
                     │ GPIO14 ◄──── AD8232 LO-
                     │ GPIO21 ◄──► MPU6050 SDA (选配)
                     │ GPIO22 ◄──► MPU6050 SCL (选配)
                     └──────────────────────┘
   面包板 + 轨 ──► GSR VCC, Pulse +, AD8232 3.3V, MPU VCC
   面包板 − 轨 ──► GSR GND, Pulse −, AD8232 GND, MPU GND
```

建议线色约定（方便学生检查）：红 = 3V3，黑 = GND，黄 = 模拟信号，蓝 = 数字/I2C。

杜邦线用量：ESP32→面包板 9 根母对公；三个模块各 3 根（AD8232 5 根）公对公或母对公，视模块排针是公是母而定。

---

## 5. 首次上机步骤（按顺序，每步有明确的"通过"标准）

**第 0 步：什么都不接，先烧录。**
接 USB → PlatformIO Upload → Serial Monitor 115200。看到：
```json
{"type":"boot","fw":"0.2.0","mpu6050":0,"max30100":0,"gsr":1,"pulse":1,"ecg":1}
{"type":"bio","seq":0,...,"lead_off":1,...}
```
说明板子、驱动、线、固件都通了。`lead_off:1` 是正常的（AD8232 还没接）。此时 `gsr_raw` 会是悬空引脚的随机值，不用管。

**第 1 步：接 3V3 / GND 到面包板电源轨。** 用万用表（如有）量轨间电压 3.3V；没有万用表就跳过。

**第 2 步：接 GSR。** 先不戴指套看 `gsr_raw`，再戴上指套看数值明显变化；深吸气憋 10 秒或心算 17×23，`gsr` 应在 1–3 秒内上升。**通过标准**：戴/不戴数值不同，且 `quality` 含 bit1（值为 2 或 6 或 7）。

**第 3 步：接 PulseSensor。** 指尖轻贴，等 3–5 秒。串口出现 `{"type":"beat","src":"ppg",...}` 逐条跳出，`hr_p` 落在 55–100。**通过标准**：beat 事件间隔与自己摸脉搏一致。

**第 4 步：接 AD8232（先不贴电极）。** `lead_off` 仍为 1，`hr_e` 为 -1。**通过标准**：贴上三个电极后 `lead_off` 变 0。

**第 5 步：贴电极。** 皮肤酒精擦拭 → 三片贴上 → 插线。模块 D1 红灯随心跳闪，串口出现 `"src":"ecg"` beat 事件。**关键**：先把笔记本电脑的**充电器拔掉**用电池运行（厂商 FAQ 第一条：隔绝 50Hz 工频干扰；同时也是安全习惯——身上贴着电极时别连市电）。

**第 6 步：看波形极性。** 发 `raw on`，用 `tools/serial_logger.py --raw` 记录 20 秒后 `plot_session.py` 出图，或直接看 PlatformIO 串口绘图。ECG 的 R 峰应该**向上**尖锐。若 R 峰向下、`hr_e` 一直 -1 或是心率的两倍（锁到 T 波上），**把红、绿两个电极对调**——固件的心跳检测只认向上的峰。

**第 7 步：统一心率切换。** 三路都在时 `hr` 应等于 `hr_e`；拔掉任一电极，`lead_off` 变 1，`hr` 在 3 秒内切到 `hr_p`。

**第 8 步：校准与 HRV。** 静坐，发 `calibrate`，30 秒后收到 `calib done`，`quality` 变 7。再静坐 30 秒，`rmssd_e` 与 `rmssd_p` 都出现正值（典型 20–80ms）并接近。

**第 9 步：稳定性。** 全部同时跑 5 分钟不断流、不重启。如果 ESP32 反复重启，先换 USB 线/USB 口。

---

## 6. 固件检查结论（firmware v0.2.0）

逐项对照厂商资料核对 [firmware/include/config.h](firmware/include/config.h) 与 [firmware/src/main.cpp](firmware/src/main.cpp)：

| 检查点 | 结果 |
|---|---|
| 引脚分配 34/35/32/27/14/21/22 | ✅ 全在 ADC1 或普通 GPIO，micro-USB 板都有；无一是启动 strapping 引脚（0/2/5/12/15） |
| ADC 12 位 + 11dB 衰减 | ✅ 满量程约 3.3V，与三个模块 3.3V 供电下的输出范围匹配 |
| AD8232 `LO±` 逻辑 | ✅ 芯片规格：电极脱落输出高电平；固件 `digitalRead() == HIGH` 即 lead off，方向正确 |
| `SDN` | ✅ 固件不控制；模块自带上拉，悬空常开 |
| 心跳检测极性 | ⚠️ `BeatDetector` 只检测向上的峰。PulseSensor 波形天然向上；AD8232 取决于 RA/LA 电极位置——贴反时需对调电极（§5 第 6 步），不改代码 |
| 输出带宽 | ✅ 10Hz bio 包 + beat 事件 + 100Hz raw 流约 3KB/s，115200 波特可承受 11KB/s |
| 没接 MPU6050 时 | ✅ 探测失败自动跳过，不会卡死 |
| 主循环 | ✅ 无 `delay()`，200Hz 采样节拍靠 `millis()`，不会被串口输出阻塞 |
| 编译 | 见下方"编译状态" |

**编译状态**：✅ 2026-09-07 本机 PlatformIO 6.1.18 `pio run -e esp32dev` 编译通过（espressif32 平台 + Arduino 框架自动下载后首次编译约 20 分钟，之后增量编译几十秒）。
- RAM 6.9%（22.6KB / 320KB），Flash 22.8%（299KB / 1.28MB）
- 无警告、无错误；`.pio/build/esp32dev/firmware.bin` 已生成，接上板子直接 Upload 即可

**不用改代码；两个可选的后续小优化**（不影响第一次上机）：
1. ECG 极性自适应：在 `BeatDetector` 里同时跟踪负向包络，自动选择幅度大的极性，就不用手动对调电极。
2. GSR 满量程告警：`gsr_raw > 4000` 持续 2 秒时在串口打一条提示，提醒指套没插紧或手太湿。

---

## 7. 与 HARDWARE.md v1.2 的差异（✅ 已于 2026-09-07 回填到 HARDWARE.md v1.3，此表留作记录）

| HARDWARE.md 原文 | 实际情况 | 影响 |
|---|---|---|
| 第 0/2 节「Grove GSR 皮肤电」 | 是**思知瑞 GSR 模块**（3 针 GND/VCC/ADC，3.5mm 指套插孔） | 接线不变（仍 3V3 + GPIO34）；名称需改 |
| 第 2 节「板上电位器可调基准，首次使用时空载调到量程中点」 | 该模块**没有电位器** | 删除此句；基线只靠固件校准 |
| 第 2 节接线表引脚名 `SIG (A0)` | 丝印是 `ADC`（V1 套件板为 `OUT`） | 改名 |
| 第 4 节电极颜色未写 | 思知瑞线色：红右胸、绿左胸、橙/黄右腹 | 补充，并加"极性反了对调红绿"说明 |
| 第 1 节「ESP32 Type-C 开发板」 | 实物为 micro-USB 版，其余相同 | 改名；补充 §2.3 面包板宽度问题 |
| 第 9 节补购清单 | 面包板/杜邦线已购 | 勾掉 |

---

## 8. 注意事项汇总（贴在工位上的版本）

1. **GSR、PulseSensor、AD8232 全部接 3V3，不接 5V。**
2. 换过接线就**先拔 USB 再动手**；带电插拔杜邦线容易短路 3V3 到 GND 导致 ESP32 重启或稳压器发热。
3. 找不到串口 → 先换一根 micro-USB **数据线**，再查驱动。
4. 烧录卡 `Connecting...` → 按住 BOOT。
5. 做 ECG 时**笔记本拔充电器**，人别碰金属桌腿。
6. R 峰向下或心率翻倍 → 对调红绿电极。
7. PulseSensor 轻贴不压；背面绝缘；避强光。
8. GSR 指套插到底；手太湿/太干都会顶格或贴零，擦干或稍等。
9. 电极贴片、指套是耗材，每人每次换新贴片，用前酒精擦皮肤。
10. 这些模块**都不是医疗器械**（厂商原话），只做教学与实验。
