# HARDWARE.md — 硬件元件介绍

> 本文档介绍项目已采购的全部硬件：每个元件是什么、工作原理、怎么接线、输出什么数据、以及这些数据在我们的系统里扮演什么角色。
> 配套架构见 [ARCHITECTURE.md](ARCHITECTURE.md)，数据采集固件见 [firmware/](firmware/)。
> 文档版本：v1.2
>
> **v1.2 变更**：移除 Gravity MAX30102（SEN0518）与 Gravity LIS2DH（SEN0224）；运动传感器改为 **MPU6050**。
> 影响：① 心率不再依赖板载算法黑盒，改由 PulseSensor/AD8232 波形逐跳算出——**4 秒延迟问题消失，HRV 主线可用**；② SEN0518 退场后 I2C 上 0x57 地址冲突消失，**单条 I2C 总线即可**；③ 失去 SpO2 佩戴质量检测，改用「心搏新鲜度」判断信号质量；④ PPG 波形检测对运动更敏感，MPU6050 运动门控的重要性上升。

---

## 0. 已有元件一览

| # | 元件 | 接口 | 测量对象 | 在系统中的角色 |
|---|---|---|---|---|
| 1 | ESP32 Type-C 开发板 | USB Type-C | — | 主控：采集、心跳检测、特征计算、JSON 输出 |
| 2 | Grove GSR 皮肤电 | 模拟 → GPIO34 | 皮肤电导（出汗） | **快速情绪通道**：秒级紧张/唤醒反应，主力实时信号 |
| 3 | PulseSensor 光电脉搏 | 模拟 → GPIO35 | PPG 脉搏波形 | **主力心率来源**：逐跳 HR + 轻量 HRV（游戏实时用） |
| 4 | AD8232 心电模块 | 模拟 → GPIO32（+ LO± → GPIO27/14） | ECG 心电波形 | **HRV 金标准**：毫秒级逐跳间隔 → RMSSD 真值；校准段/实验段使用 |
| 5 | MPU6050 六轴传感器 | I2C `0x68` | 三轴加速度（+陀螺仪，暂不用） | **裁判**：检测手部大动作，标记数据不可信时段 |
| 6 | 裸 MAX30100 | I2C `0x57`（**默认停用**） | 原始 IR/RED PPG | 备用/对比实验；多数板需改上拉电阻（见第 6 节） |

> ⚠️ **两个命名澄清**：
> 1. Grove GSR 的 A0 输出**不是心率波形**，而是**皮肤电导**（EDA）——反映手指微出汗程度，是情绪唤醒指标。
> 2. PulseSensor 电商标题常写「心电脉搏」，但它是**光电（PPG）**传感器，测指尖血容量脉动；真正的**心电（ECG）**是 AD8232（测心脏电信号）。两者波形来源完全不同，正好可以对比教学。

**心率策略（v1.2）**：固件同时跑 PulseSensor 与 AD8232 的波形心跳检测，输出统一心率 `hr`，优先级 **ECG > PPG > MAX30100**——静息/校准段 ECG 最准就用 ECG；游玩时按键肌电会干扰 ECG，`hr` 自动回落到 PulseSensor。三路各自的读数（`hr_e` / `hr_p` / `hr_m`）也都单独记录，供赛后互相校验。

---

## 1. ESP32 Type-C 开发板（主控）

### 是什么
一块带 Wi-Fi 和蓝牙的微控制器开发板，本项目的「大脑」。所有传感器接在它上面，它负责采集波形、实时检测心跳、计算特征，再通过 USB 串口（或未来的 Wi-Fi UDP）把数据以 JSON 格式发给电脑。

### 关键规格
| 项目 | 数值 | 对本项目的意义 |
|---|---|---|
| 主频 | 240 MHz 双核 | 200Hz 双通道波形采样 + 心跳检测 + JSON 序列化毫无压力 |
| 工作电压 | 3.3V（USB 5V 输入，板载稳压） | **所有传感器统一从 3V3 引脚取电** |
| ADC | 12-bit，ADC1（GPIO32–39）+ ADC2 | 三路模拟传感器全接 **ADC1**——ADC2 在 Wi-Fi 开启时不可用 |
| I2C | GPIO21/22（可自定义） | MPU6050（0x68）+ MAX30100（0x57，选配）共挂一条总线，无地址冲突 |
| 接口 | USB Type-C | 供电 + 烧录 + 串口传输三合一 |

### 使用注意
- ADC 输入上限 **3.3V**，超过会损伤引脚——GSR 必须用 3.3V 供电。
- 第一次连电脑需装 USB 驱动（CH340 或 CP210x，看板载串口芯片丝印）。
- 烧录失败时按住 BOOT 键再点烧录（部分板子需要）。

---

## 2. Grove GSR 皮肤电传感器

### 是什么
测量皮肤电导（GSR / EDA）的模拟传感器，配两个指部绑带电极。**整个项目里最直接反映"紧张程度"的传感器，系统的主力实时信号。**

### 工作原理：情绪性出汗
```
   紧张/兴奋/惊吓 → 交感神经兴奋
        ↓
   手指汗腺分泌微量汗液（自己感觉不到）
        ↓
   汗液含电解质 → 皮肤导电性上升
        ↓
   指环电极间测量电导变化 → A0 输出电压 → ESP32 ADC
```
汗腺**只受交感神经支配**（无副交感拮抗），所以 GSR 是心理学公认最"干净"的唤醒度指标——测谎仪的核心传感器。情绪刺激后 **1–3 秒**内可见反应。

### 接线
| 传感器引脚 | 接 ESP32 | 说明 |
|---|---|---|
| VCC | **3V3（不要接 5V！）** | 5V 供电时输出可能超过 ADC 的 3.3V 上限 |
| GND | GND | |
| SIG (A0) | **GPIO34** | ADC1_CH6，纯输入引脚 |

- 电极佩戴：两个指环套在**同一只手的食指与中指**第二指节。
- **建议戴在不按键的那只手**，否则按键动作引入伪影。
- 板上电位器可调基准，首次使用时空载调到输出约在量程中点。

### 这些数值如何帮助我们的系统

| 特征 | 计算方式 | 系统用途 |
|---|---|---|
| `gsr_delta` | `(当前值 - 静息基线) / 静息基线` | **五个状态判断规则每一条都用到** |
| `gsr_slope` | 最近 15 秒线性斜率 | 捕捉「正在紧张起来」的趋势 |
| 长期漂移 | EMA 慢速基线跟踪 | 扣除汗液累积/电极极化的缓慢漂移 |

### 已知限制
- 个体差异极大 → **一切用相对基线变化量，绝不用绝对值**。
- 温度、湿度、运动都会干扰 → 校准环节 + MPU6050 运动门控。
- 电极汗渍老化 → 多备几对指环电极带。

---

## 3. PulseSensor 光电脉搏传感器（主力心率来源）

### 是什么
最经典的开源教学心率传感器（绿光 LED + 光敏元件的小圆板），输出连续的 PPG 模拟波形。**v1.2 起它是游戏实时心率的主力来源。**

### 工作原理：PPG（光电容积脉搏波）
```
   绿光 LED 照进指尖皮肤
        ↓ 心脏每跳一次，指尖血容量脉动一次
   反射光强度随脉搏波动 → 光敏元件 → 模拟电压波形
        ↓ ESP32 以 200Hz 采样
   固件 BeatDetector：去基线 → 自适应阈值 → 找波峰
        ↓
   逐跳间隔 IBI → 心率 hr_p（每跳更新）→ RMSSD（HRV）
```

### 接线
| 引脚（线色） | 接 ESP32 | 说明 |
|---|---|---|
| +（红） | 3V3 | 3.3V 下工作正常，输出不超 ADC 量程 |
| -（黑） | GND | |
| S（紫） | **GPIO35**（ADC1_CH7） | 模拟 PPG 波形 |

- 佩戴：指尖（配防滑贴/魔术贴）或耳垂夹，**戴在非按键手**。
- 力度关键：太紧压闭血管没信号，太松漏光噪声大——「轻贴」最好。

### 这些数值如何帮助我们的系统

| 特征 | 系统用途 |
|---|---|
| `hr_p`（逐跳心率） | 统一心率 `hr` 的常规来源（游玩时 ECG 受肌电干扰，靠它撑）→ `hr_delta` → 状态判断的慢速通道 |
| `rmssd_p`（HRV） | 轻量 HRV：压力上升 → RMSSD 下降，是独立于心率的第二证据 |
| 波形本身 | **波形级信号处理的最佳教材**：学生亲眼看到"从一条波浪线到心率数字"的全过程 |

### 已知限制
- 运动伪影敏感：甩手、按压变化都会让波峰错乱 → MPU6050 门控 + IBI 生理范围过滤（固件已内置）。
- 光学串扰：避免强光直射；与其他光学传感器（MAX30100）别贴在同一根手指。

---

## 4. AD8232 心电模块（ECG）——HRV 金标准

### 是什么
单导联心电前端芯片模块，通过 3 个贴片电极直接测量**心脏的电信号**（不是光学间接测量）。R 峰时刻极其锐利，逐跳间隔可精确到毫秒——HRV 测量的金标准。

### 工作原理
心脏每次收缩由一次电去极化波驱动，电信号传导到体表，幅度约 1mV。AD8232 放大约 1100 倍并滤波，输出 0–3.3V 的模拟心电波形；固件 200Hz 采样后用 BeatDetector 找 R 峰。

### 接线
| 模块引脚 | 接 ESP32 | 说明 |
|---|---|---|
| 3.3V | 3V3 | |
| GND | GND | |
| OUTPUT | **GPIO32**（ADC1_CH4） | 模拟心电波形 |
| LO+ | **GPIO27** | 导联脱落检测（电极掉了输出高电平） |
| LO- | **GPIO14** | 同上 |
| SDN | 不接（或接 3V3） | 关断控制，悬空即常开 |

**电极贴法**（两种都可以，采集时选一种并记录在 meta 里）：
- 胸贴法（信号最好）：RA 右锁骨下、LA 左锁骨下、RL 右下腹
- 手臂法（方便）：RA 右前臂内侧、LA 左前臂内侧、RL 任一手腕外侧——**游玩中按键会引入肌电噪声**，所以 ECG 主要用于静息校准段与实验采集段

### 这些数值如何帮助我们的系统

| 特征 | 系统用途 |
|---|---|
| `hr_e` | 统一心率 `hr` 的最高优先来源（静息段最准） |
| `rmssd_e` | **HRV 真值**：(a) 校验 `rmssd_p` 准不准；(b) 给 Nick 的心理学分析提供金标准证据 |
| `lead_off` | 电极脱落自动标记，坏数据不进分析 |

**耗材提醒**：一次性心电贴片电极是消耗品（每人每次 3 片），采集 10 人次要备 30+ 片，务必补购。

---

## 5. MPU6050 六轴传感器（运动门控）

### 是什么
最普及的 MEMS 六轴模块（三轴加速度 + 三轴陀螺仪）。本项目**只用加速度部分**做运动检测；陀螺仪暂不用（未来可做手势/体感玩法）。

### 工作原理：MEMS 微机械结构
芯片内部微米级硅质量块悬挂在弹簧结构上，加速时质量块位移改变电容，测电容换算加速度。静止时也测得到重力（1g），据此可算倾斜姿态。

### 配置与接线
| 项目 | 本项目配置 |
|---|---|
| 量程 | ±2g（手部动作足够，精度最高） |
| 滤波 | 芯片内置 DLPF 44Hz，滤掉高频振动 |
| 读取频率 | 固件 25Hz |
| 接口 | I2C（GPIO21/22），地址 `0x68`（AD0 悬空/接地）；固件自动尝试 `0x69`（AD0 接高） |

| 模块引脚 | 接 ESP32 |
|---|---|
| VCC | 3V3 |
| GND | GND |
| SDA | GPIO21 |
| SCL | GPIO22 |
| AD0 / INT / XDA / XCL | 不接 |

- 驱动：固件内置最小寄存器驱动（不依赖第三方库）。**注意 MPU6050 上电默认在睡眠模式**，固件已自动唤醒（写 PWR_MGMT_1=0）。

### 这些数值如何帮助我们的系统

| 特征 | 计算方式 | 系统用途 |
|---|---|---|
| `acc`（运动量） | `abs(sqrt(x²+y²+z²) - 1g)` 平滑后 | **运动门控**：`acc > 0.8g` 时冻结状态判断——PPG/GSR/ECG 全都怕动作伪影，v1.2 硬件下这个门控比以前更关键 |
| 运动统计 | 30 秒窗口动作总量 | 赛后分析：区分「紧张导致表现差」和「乱动导致数据差」 |
| （延伸） | 陀螺仪 + 姿态 | 未来体感玩法：拍打节奏、手势触发 |

- 佩戴位置：与 PulseSensor 同一侧（手背/手腕），测到的运动才代表"污染 PPG 的那个运动"。

---

## 6. 裸 MAX30100 模块（选配备用）

### 是什么
数字式 PPG 芯片裸模块（无板载算法），I2C 输出原始 IR/RED 光强，配开源库（oxullo `MAX30100lib`）在 ESP32 上算心率。定位是 **PulseSensor 的备份**与「模拟 vs 数字 PPG」对比实验材料。

### ⚠️ 已知坑：上拉电阻
多数廉价 GY-MAX30100 板的 I2C 上拉电阻接到了 1.8V 轨，直接用会通信失败或读数全 0。修法：拆掉板上 3 颗 4.7kΩ 电阻，在 SDA/SCL 与 3.3V 之间外接 4.7kΩ 上拉（网上搜 "MAX30100 上拉电阻改装" 有大量图文）。**固件默认停用（`ENABLE_MAX30100 0`），改装并验证后再启用**——不影响其他任何传感器。

### 接线
VIN→3V3，GND→GND，SDA→GPIO21，SCL→GPIO22（与 MPU6050 共总线，地址 `0x57` 无冲突）。

### 数值角色
`hr_m` 参与统一心率 `hr` 的兜底（优先级最低）；三种心率方案（模拟 PPG / 数字 PPG / ECG）横向对比是很好的课程报告素材。

### 三种心率来源对比

| 来源 | 类型 | 更新速度 | IBI/HRV | 抗运动 | 开发难度 | 项目角色 |
|---|---|---|---|---|---|---|
| PulseSensor | PPG（模拟波形） | 每跳（约 1s） | ✅ | 弱 | 低（固件已内置检测器） | **主力**：游戏实时心率 |
| AD8232 | ECG（电信号） | 每跳 | ✅✅ 金标准 | 中（怕肌电） | 中 | **真值**：校准段/实验段 HRV |
| MAX30100 | PPG（数字原始） | 每跳 | ✅ | 弱 | 中高（需改板） | 备用/对比实验（默认停用） |

---

## 7. 整机接线总表（v1.2）

### 引脚分配一览

| ESP32 引脚 | 连接 | 类型 |
|---|---|---|
| 3V3 | 所有传感器 VCC（**GSR 严禁接 5V**） | 电源 |
| GND | 所有传感器 GND（共地） | 电源 |
| GPIO21 | I2C SDA：MPU6050（0x68）+ MAX30100（0x57，选配） | 数字 |
| GPIO22 | I2C SCL | 数字 |
| GPIO32 | AD8232 OUTPUT（心电波形） | ADC1 |
| GPIO34 | Grove GSR SIG | ADC1（纯输入） |
| GPIO35 | PulseSensor S | ADC1（纯输入） |
| GPIO27 | AD8232 LO+（导联脱落） | 数字输入 |
| GPIO14 | AD8232 LO-（导联脱落） | 数字输入 |

### 总线拓扑

```
ESP32 ──┬── I2C (GPIO21/22) ──┬── MPU6050  @0x68
        │                     └── MAX30100 @0x57 (选配, 默认停用)
        │
        ├── ADC1: GPIO34 ← GSR    GPIO35 ← PulseSensor    GPIO32 ← AD8232
        ├── 数字: GPIO27/14 ← AD8232 LO+/LO-
        │
        └── USB Type-C ──→ 电脑 (串口 115200, NDJSON)

供电：所有传感器统一 3V3、共地（后续如加 OLED 0x3C 直接挂同一 I2C）
```

### 佩戴方案建议（传感器全在**非按键手** / 左手，右手按 D F J K）

| 部位 | 元件 |
|---|---|
| 左手食指 + 中指 | GSR 指环电极 |
| 左手无名指或小指指尖 | PulseSensor（魔术贴固定，遮光） |
| 左手手背/手腕 | MPU6050 |
| 胸口或双前臂 | AD8232 三贴片（校准段/实验段） |

---

## 8. 数据 → 系统的完整映射

```
Grove GSR   ──→ 电导 (50Hz)    ──→ gsr_delta        ──→ ★ 快速唤醒水平（每条规则都用）
                               ──→ gsr_slope        ──→ ★ "正在紧张起来"触发器
PulseSensor ──→ PPG波形(200Hz) ──→ hr_p, rmssd_p    ──→ 实时心率主力 + 轻量HRV
AD8232      ──→ ECG波形(200Hz) ──→ hr_e, rmssd_e    ──→ 心率/HRV 金标准（静息段）
                               ──→ lead_off         ──→ 坏数据自动标记
MAX30100    ──→ 原始IR/RED     ──→ hr_m             ──→ 兜底 + 对比实验（选配）
MPU6050     ──→ 加速度(25Hz)   ──→ acc              ──→ 运动门控（防伪影误判）

  固件融合: hr = ECG > PPG > MAX30100 优先级 → hr_delta（相对静息基线）

                + Unity 侧游戏表现（accuracy / miss / combo break / 反应时方差）
                ═══════════════════════════════════════════
                →  calm / focused / nervous / frustrated / overloaded
                →  动态调整 note speed / density / complexity
```

一句话分工：**GSR 说"现在多紧张"，心率/HRV 说"紧张持续了没有、自律神经谁在踩油门"，ECG 保证这些数字有真值可对照，MPU6050 说"这些数据能不能信"，游戏表现说"紧张是专注还是崩溃"。**

---

## 9. 建议补购清单

### 必备（第一优先）

| 元件 | 用途 | 参考价 |
|---|---|---|
| **一次性心电贴片电极（50 片装）** | AD8232 耗材，每人每次 3 片，10 人次采集要 30+ 片 | ¥15–25 |
| 面包板 + 杜邦线（公母/母母各一把） | 原型接线 | ¥15–25 |
| 魔术贴绑带 / 医用胶带 | 固定 PulseSensor 于指尖、走线固定 | ¥10 |
| 备用 GSR 指环电极（2–3 对） | 电极汗渍老化后更换 | ¥10–20 |
| 酒精棉片 | 每位受试者使用前清洁电极和皮肤 | ¥5 |
| 4.7kΩ 电阻若干 | MAX30100 上拉改装用（若决定启用它） | ¥2 |

### 强烈建议

| 元件 | 用途 | 为什么值得加 | 参考价 |
|---|---|---|---|
| **轻触按钮 x2** | **实验事件标记器** | 受试者感到紧张/挫折的瞬间按一下，数据流打上 marker → 给机器学习提供**逐时刻 ground truth 标签**，性价比最高的补充硬件 | ¥1 |
| 0.96" OLED 屏（SSD1306, I2C 0x3C） | 脱机调试显示 | 不接电脑直接看读数；挂现有 I2C 总线即可 | ¥10–15 |
| 振动马达模块 | 触觉反馈 | 状态切换提示 / 深呼吸引导节拍器 | ¥3–5 |
| RGB LED / WS2812 | 状态可视化 | 五色实时显示 bio_state，汇报展示效果好 | ¥3–5 |

### 可选（进阶阶段）

| 元件 | 用途 | 参考价 |
|---|---|---|
| 锂电池 (1000mAh) + TP4056 充电模块 | 脱离 USB 线，配合 Wi-Fi UDP 无线化 | ¥20–30 |
| MLX90614 红外测温（I2C 0x5A） | 皮肤温度通道（压力下末梢温度缓慢下降） | ¥25–40 |
| 第二块 ESP32 | 备援 / 双受试者对照 | ¥25–40 |

---

## 10. 上电自检清单（P0 硬件验收用）

按顺序执行，全部通过才算硬件就绪（对应 [firmware/](firmware/) 固件与 `tools/serial_logger.py`）：

1. [ ] ESP32 接电脑，设备管理器出现 COM 口，烧录 Blink 成功
2. [ ] 按第 7 节接线（**GSR 接 3.3V**；MPU6050 挂 GPIO21/22）
3. [ ] 烧录本项目固件，boot 行显示 `"mpu6050":1`
4. [ ] GSR：空载读值稳定；戴上电极读值变化；心算/憋气 10 秒能看到 `gsr` 爬升
5. [ ] MPU6050：静置 `acc` 接近 0；甩动明显增大
6. [ ] PulseSensor：指尖轻贴，串口出现 `"src":"ppg"` 的 beat 事件，`hr_p` 在合理范围（55–100）
7. [ ] AD8232：贴好三电极后 `lead_off` 变 0，出现 `"src":"ecg"` 的 beat 事件；拔掉任一电极 `lead_off` 变 1
8. [ ] 统一心率：`hr` 有值且与 `hr_e`/`hr_p` 一致；摘掉 ECG 电极后 `hr` 自动切换到 PPG 来源
9. [ ] 静坐 1 分钟，`rmssd_e` 与 `rmssd_p` 输出正值（典型 20–80ms）且两者接近
10. [ ] 全传感器同时运行 5 分钟，10Hz 数据包不断流、ESP32 不重启（若重启查供电与接线）
